{
  "name": "load-range-unit-fold",
  "description": "A unit nested inside Load.value.range must fold up to the sibling Load.unit on normalization (§8.3 step 2), symmetrically to the absolute-Target case. A 20-30 kg band written with the unit inside `range` must normalize equal to the same band with `unit` on `Load`.",
  "specRefs": ["§5.12", "§8.3"],
  "kind": "equivalent",
  "a": {
    "id": "wu-band-row-1",
    "recordType": "WorkUnit",
    "subject": "subj-001",
    "exerciseRef": "band-row",
    "scoring": "reps",
    "prescription": {
      "reps": 12,
      "load": { "value": { "range": { "min": 20, "max": 30, "unit": "kg" } } }
    }
  },
  "b": {
    "id": "wu-band-row-1",
    "recordType": "WorkUnit",
    "subject": "subj-001",
    "exerciseRef": "band-row",
    "scoring": "reps",
    "prescription": {
      "reps": 12,
      "load": { "unit": "kg", "value": { "range": { "min": 20, "max": 30 } } }
    }
  }
}
