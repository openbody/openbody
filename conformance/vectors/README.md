# Minimum core vectors

The conformance bar (SPEC §8.3). Each vector is a JSON record (or record set) plus an
assertion about its canonical form — e.g. two encodings asserted equal, or an
input→normalized pair. Authored once a reference implementation/validator exists to
execute the round-trip; this placeholder marks the intended home.
