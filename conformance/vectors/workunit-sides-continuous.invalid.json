{
  "name": "workunit-sides-continuous",
  "description": "`sides` MUST NOT be present when `scoring: continuous` (§5.5): `continuous` has no single fixed primary metric for `sides.count` to multiply — `sides` is designed around a fixed reps/time/distance/energy atom (e.g. a per-side plank or carry), not a continuously-measured effort like a run.",
  "specRefs": ["§5.5", "§5.8"],
  "kind": "invalid",
  "record": {
    "id": "wu-run-sides-invalid",
    "recordType": "WorkUnit",
    "subject": "subj-001",
    "exerciseRef": "run.outdoor",
    "scoring": "continuous",
    "performance": {
      "distance": { "absolute": { "value": 5000, "unit": "m" } },
      "sides": { "count": 2 }
    }
  }
}
